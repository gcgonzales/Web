
<div id="divListaQuedadas" class="marginTopBody">
    @*<div datepicker datepicker-class="test-custom-class" date-min-limit="2016/02/28" date-max-limit="2016/03/03" date-typer="true">
        <input ng-model="Quedada.FechaAlta" type="text" class="angular-datepicker-input" />
    </div>*@
    <div class="container" ng-init="BuscarQuedadas()">
        <div class="row">
            <div class="col-xs-12">
                <div class="btn btn-danger alignRight" ng-click="BajaSeleccionadosQuedada()">Baja a seleccionados</div>
                <div class="btn btn-default alignRight" ng-click="AltaQuedada()">Nueva quedada</div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12 col-sm-6 TemaForo" ng-repeat="quedada in Quedadas" ng-click="GetQuedada(quedada.Id)">
                <div class="row backGroundForo1">
                    <div class="col-xs-1">
                        <input type="checkbox" ng-model="quedada.Seleccionado" />
                    </div>
                    <div class="col-xs-11">
                        <h2>{{quedada.Titulo}}</h2>
                    </div>
                </div>
                <div class="row cuerpoTemaForo">
                    <div class="col-xs-2">
                        <img alt="persona" ng-src="{{(quedada.RutaFotoPrincipal != null && quedada.RutaFotoPrincipal != '') ? quedada.RutaFotoPrincipal : '/Content/images/person.jpg'  }}" ng-click="MostrarUsuario(mensajeForo.IdUsuarioAlta)" class="photoLogado" />
                    </div>
                    <div class="col-xs-10">
                        <h2>{{quedada.Titulo}}</h2>
                    </div>
                    <div class="col-xs-10">
                        <h2>{{quedada.Resumen}}</h2>
                    </div>
                </div>
            </div>
        </div>

    </div>

</div>

@Html.Partial("_DetalleQuedada")